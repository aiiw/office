```
若需要查找问号“?”，则在查找内容文本框中输入“~?”。“?”为通配符，代替单个任意字符，如果直接查找，会找到包含数据的所有单元格。
```

```
如果要查找特殊字符例如
0D是回车符（Carriage Return），它的十六进制表示为0D。在ASCII编码中，它通常表示为\r。
0A是换行符（Line Feed），它的十六进制表示为0A。在ASCII编码中，它通常表示为\n。

0D的十进制表示为13。
0A的十进制表示为10。

可以直接按住 ALT输入10 这样可以查找特殊字符

但是实际上wps可以直接搜索出特殊字符
```

