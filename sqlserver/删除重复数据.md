```

WITH CTE AS (
    SELECT 
           ROW_NUMBER() OVER (PARTITION BY ApplyNo ORDER BY empid ) AS RowNumber,*
    FROM T_HR_Absence
     WHERE BeginTime > '2023-08-31 07:30:00.000' -- 日期类型的比较
)
delete FROM CTE
WHERE RowNumber > 1;
```

