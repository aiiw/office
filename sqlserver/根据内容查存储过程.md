SELECT 
    name AS [Stored Procedure Name], 
    definition AS [Procedure Definition]
FROM 
    sys.sql_modules m
INNER JOIN 
    sys.objects o ON m.object_id = o.object_id
WHERE 
    o.type = 'P' -- 筛选出存储过程
    AND m.definition LIKE '%00 11%' -- 替换 test 为你要查找的表名
    