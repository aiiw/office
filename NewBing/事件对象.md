```
也说是说即便将事件绑定函数后，函数的第一个参数，如果在函数体，没有调用事件的方法或属性，它仍然是一个普通的参数

2023/4/13 16:46:50

是的，您说得没错。当您将事件绑定到一个函数时，第一个参数通常是事件对象（例如，MouseEvent或KeyboardEvent）。但是，如果您在函数体中没有使用事件对象的任何方法或属性，那么它仅仅是一个普通的参数，并且您可以像处理任何其他参数一样来处理它。这意味着您可以将任何类型的值作为第一个参数传递给函数，而不必担心它是否包含事件对象。




2023/4/13 16:47:30

那如果我将函数的第二个参数，将它当成是事件对象在函数体调用 ，可行？

2023/4/13 16:47:41

如果您将函数的第二个参数当作事件对象在函数体中进行调用，这是不可行的。因为在绑定事件时，约定俗成的规则是将事件对象作为第一个参数传递给函数。如果您将事件对象作为第二个参数传递，并且在函数体中使用它，那么您将无法访问事件对象的属性和方法，因为它不会被正确地传递给函数。

当您绑定事件时，浏览器会自动将事件对象作为第一个参数传递给处理函数。这是由于JavaScript事件模型的机制所决定的。所以，如果您想在函数体中访问事件对象，您必须将函数的第一个参数声明为事件对象。
```

