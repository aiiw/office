在 MySQL 和 SQL Server 中，连接字符串（拼接字符串）使用不同的运算符：

- **MySQL**: 使用 `CONCAT()` 函数。例如：

  ```
  
  SELECT CONCAT('Hello', ' ', 'World');
  ```

- **SQL Server**: 使用 `+` 运算符。例如：

  ```
  SELECT 'Hello' + ' ' + 'World';
  ```